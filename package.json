{
  "name": "imds-social-jules-enock",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp -r www/* dist/"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/cli": "^7.4.0",
    "@capacitor/core": "^7.4.0"
  }
}
